import React from 'react';
import { Layout } from '@ui-kitten/components';
import { styles } from './IntroVideo.style';

export const IntroVieo = () => {
    return (
        <Layout style={styles.container}>

        </Layout>
    )
}